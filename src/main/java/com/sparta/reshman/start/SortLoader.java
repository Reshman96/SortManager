package com.sparta.reshman.start;

public class SortLoader {

    public static void start() {
        /*
        1. Request which sorter the user wants to use (BinarySorter, BubbleSorter, InsertionSorter, MergeSorter, QuickSorter)
        2. Request the size of the array user wants to generate
        3. "Sorting using (sortername)"
            "Before sorting:"
            "(array/array list?)"
            "After sorting:"
            "(array/array list?)"
            "Time taken: (nanoseconds) nanoseconds"
         */

    }
}
