package com.sparta.reshman;

public class PerformanceTester {
    /*
    Take an array and run every single sort algorithm against it.
    And give time taken in nanoseconds (or milliseconds) to see how fast they are.
     */
}
